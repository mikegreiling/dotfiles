# .zprofile is sourced on login shells and before .zshrc. As a general rule, it should not change the
# shell environment at all.
#
# Note: Homebrew path configuration has been moved to ~/.zshenv since .zshenv is always loaded
# by all shell types (login/non-login, interactive/non-interactive)
